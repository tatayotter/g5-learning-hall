// lib/weekdays.ts
// Pulled out of Dashboard.tsx so board/gauntlet-related pieces split into
// their own components (components/dashboard/board/*) can share the same
// list instead of each re-declaring the literal (drift risk).
export const WEEKDAYS = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"];
